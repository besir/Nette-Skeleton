common:
	parameters:
		database:
			host: 127.0.0.1
			username: change
			password: change
			database: change
			lazy: FALSE

	php:
		date.timezone: Europe/Prague
		# zlib.output_compression: yes

	nette:
		application:
			errorPresenter: Error

		session:
			expiration: 14 days
			autoStart: true

	includes:
		- factories.neon
		- services.neon
		- forms.neon
		- model.dao.neon
		- model.service.neon
		- components.neon

production < common:

development < common:
